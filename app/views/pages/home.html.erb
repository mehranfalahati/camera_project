<h1>Camera House</h1>

<% if @current_user.present? %>
    <h2>Welcome <%= @current_user.email %></h2>
    <p>
        <% @random.each do |camera|%>
            <h1><%= camera.name %></h1>
            <%= cl_image_tag(camera.image) %>
        <% end %>
    </p>
<% end %>
